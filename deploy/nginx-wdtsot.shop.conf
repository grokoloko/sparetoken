# Isolated vhost for wdtsot.shop — do not replace the default site.
# App: 127.0.0.1:8787
# Canonical host: wdtsot.shop

server {
    listen 80;
    listen [::]:80;
    server_name www.wdtsot.shop;
    return 301 http://wdtsot.shop$request_uri;
}

server {
    listen 80;
    listen [::]:80;
    server_name wdtsot.shop wdtsot.150.136.116.206.nip.io 150.136.116.206.nip.io 150.136.116.206;

    client_max_body_size 32k;

    location /api/ {
        proxy_pass http://127.0.0.1:8787;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
        proxy_read_timeout 180s;
        proxy_send_timeout 180s;
    }

    location / {
        proxy_pass http://127.0.0.1:8787;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
