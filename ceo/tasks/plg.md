# Product-led — aparecer (função séria)

O site tem que **segurar o clique**. Anônimo até pagar. Depois o `wdtsot-XXXX` é o login. O mesmo `?code=` é o convite.

## O que vira tarefa (gradual, com dado)

- Popup curto: “paga o Pix e leva o código” — um passo, sem cadastro.
- Depois do código: “manda este link” = `/?code=DELE`.
- Centavos de compute por amigo que **pagou** (charge fechado).
- Ao juntar **R$5** de indicação: escolhe Pix (o que o conta.vc do fuzzy — e depois de outros — tiver). 10% por sessão.
- Primeiro da fila: **fuzzy**.

Rápido. Fácil. Sem segundo checkout. Sem PII na landing.

Cada pulso que tiver um número (cliques, charges fechados, convites colados) **abre uma task** no 7-day. Sem número, não inventa modal.
